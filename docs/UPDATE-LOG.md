# 更新日志

## 2026-08-01 · 桌面端范围收口

### 本次变更

- 产品改为仅支持桌面端，设计画布固定为 1180px，不再提供移动导航或手机专用断点体验。
- 删除汉堡菜单、移动侧栏、移动端 AI 浮动入口和底部抽屉；AI 助手统一由桌面顶栏打开。
- 保留桌面窗口缩放和组件内部防溢出能力，但不再进行手机截图、断点适配或移动端验收。

## 2026-08-01 · V2.5.2 导出附件信息精简

### 本次变更

- PDF 与 Word 的核心结果缩略图不再显示或写入原始文件名。
- 删除科研档案模板中的附件路径、版本、大小和 SHA-256 明细，所有报告模板采用同一隐私规则。
- 每条实验记录的附件区域仅保留最多两张匿名缩略图和一个 `record-N` 完整实验文件夹摘要。
- 回归测试同时扫描 Word XML、关系文件、PDF 可见段落和 PDF 原始字节，防止具体附件名重新进入导出文件。

## 2026-08-01 · V2.5.1 实验报告导出可读性修复

### 本次变更

- 提高 Word 与 PDF 实验报告的标题、表头、正文辅助文字和核验提示对比度；PDF 深色表头统一使用白字。
- 单条记录和完整实验报告的“原始数据与附件”统一最多展示两张核心结果缩略图。
- 其余原始文件不再逐项列出文件名、哈希与元数据，只显示 `record-N` 文件夹名称和文件总数，完整内容仍可在文件中心查看。
- 增加导出回归测试，防止附件长清单和低对比度 Word 样式重新出现。

## 2026-08-01 · V2.5.0 文件批量管理与稳定性修复

### 本次变更

- 文件中心和单个实验文件页改为服务器分页，可选择每页 20、50 或 100 个文件。
- 增加“全选本页”和“全选全部匹配结果”两级选择；跨页选择可统一执行批量编辑、ZIP 下载和移入回收站，并在后端重新校验当前用户与筛选范围。
- 批量编辑支持分类、标签和说明；大批量 ZIP 改用临时缓冲文件，减少内存占用。
- 周报标题索引改为分页加载，指定周报仍可跨页直接打开；修复周报数据库表缺失导致的 500 错误。
- 回收站重构为按资源类型切换的单列表，只查询当前类型并支持搜索、分页和每页数量设置。
- AI 助手移除左右贴边功能，保留自由移动、显式缩放抓手、最大化与恢复；窗口会自动限制在当前视口内，关闭按钮始终可见。
- 本地启动时自动执行带备份的数据库迁移，升级前保留 SQLite 备份且不移动实验附件。

## 2026-08-01 · V3 信息架构、周报资料库与响应式界面

### 本次变更

- 明确产品定位为本地优先的科研资料管理系统，业务主线统一为“项目 → 实验计划 → 实验执行 → 过程记录 → 原始数据”。
- 将“汇报”收口为“周报资料库”：支持本地 PPT/PDF 归档、周报日期、项目归类、标题检索、反馈标注、修改日常和本地文件夹入口。
- 新增全局实验报告卡片流和文件中心；报告只显示最多两张结果缩略图，其余文件通过项目文件夹链接查阅。
- 完成桌面端顶部工具栏、周报双栏阅读区、报告卡片流、文件中心列表和移动端单栏布局；移动端 AI 助手改为右下角入口与底部半屏抽屉。
- AI 输入框支持自动增高、快捷提示同步字符计数，`Ctrl K` 在助手关闭时聚焦当前页面搜索框。
- 修复文件中心批量下载的重名文件路径处理，确保重命名文件仍保留所属实验目录。

### 架构说明

完整的信息架构和页面职责见 [`docs/UX-IA-V3.md`](UX-IA-V3.md)。网盘、飞书和外部协同暂不进入核心链路，后续应通过文件中心的存储适配层扩展。

## 2026-07-31 · 移除论文模块与助手界面收口

### 本次变更

- 移除论文、审稿意见和统计页，不再保留论文路由、导航、任务分类或汇报主题。
- 数据库升级时删除 `paper`、`reviewer_comment` 表，并清理旧论文分类任务和 PPT Skill 主题数据。
- AI 助手增加 API 设置快捷入口，持续显示实验范围和知识库范围，消息角色、提案和输入区层级重新整理，移动端改为单栏抽屉式历史会话。

## 2026-07-24 · V2 科研工作流与 AI 配置收口

### 本次目标

本轮把产品主线统一为：

`科研项目 → 实验计划 → 实验执行 → 过程记录 → 文件与导出`

实验计划回答“准备做什么”，实验执行表示“一次实际开展”，过程记录只描述该次执行中“发生了什么”。内部旧模型名暂时保留以兼容已有数据库，但用户界面、AI 提示和导出文件统一使用“实验执行 / 过程记录”。

### API 预设与模型能力

- API 设置只保留可命名、保存和切换的预设，不再展示容易混淆的单连接兼容表单。
- 移除视觉、嵌入和图像模型的手工输入框；一个预设选择一个主模型，能力由模型列表和接口元数据自动描述。
- 支持从兼容服务的 `/models` 接口拉取模型，兼容 `data`、`models`、嵌套对象、直接数组、字符串模型项以及 `id`、`name`、`model` 等常见字段。
- 模型列表以小图标展示视觉、推理、工具调用和联网搜索能力，并区分“接口声明、保守推测、明确不支持、未知”，不会把兼容服务的模型名称误判成已支持联网。
- 支持 camelCase 能力元数据，例如 `supportsFunctionCalling` 和 `inputModalities`。
- 账号中已启用的默认预设是 AI 唯一连接来源；没有可用预设时保持未连接，不再回落到环境变量。
- 模型能力快照随预设持久化，并同时驱动 API 页面图标和助手联网开关；停用的默认预设不再显示为“当前使用”。
- 旧 `ApiSetting` 会迁移或合并到预设后清理；已有预设不会被旧配置替换。API Key 继续加密保存，页面只显示掩码。

### 导航与个人菜单

- 主导航突出科研项目、实验计划、模板、样本和汇报，不再放置独立 AI 页面。
- AI 助手只保留全局悬浮框，可在当前科研页面继续对话和发起经确认的操作。
- API 设置保留在侧栏本地工作区入口；旧外观切换、暗色模式和自定义背景已退役，界面固定为统一的 Swiss 研究工作台。
- 模板中心保持一级入口，方案模板和记录模板都能在调用前直接创建、预览、编辑、复制和删除。

### AI 科研助手

- AI 现在可以创建和修改科研项目，也可以从当前实验计划新建一次实验执行；所有写入先展示字段差异，用户确认后才写入数据库，并保留撤销信息。
- AI 可在科研项目、实验计划、实验执行和过程记录上下文中管理字段、步骤、参数、样本使用、过程记录和附件元数据。
- 新建实验计划时会在差异确认区明确选择所属科研项目；账号下有多个项目时不再静默写入最早创建的项目。
- 跨页面继续旧会话时，发送前预览与实际生成共用当前页面的已验证上下文，不会出现“预览 A、实际修改 B”。
- 项目页会默认选中该项目下的活动实验历史，可直接比较执行、总结过程记录或生成项目周报。
- AI 历史范围可先按实验计划全选，也可精确勾选某几次实验执行；提示上下文和引用会携带执行编号、实际参数及执行 ID，不同重复执行不再混成一组记录。
- 实验执行修改增加枚举、布尔值、日期区间、跨执行记录、软删除父级和撤销冲突校验。
- AI 快照覆盖过程记录生命周期、定稿时间、来源消息、参数、附件状态和修订关系；页面在保存后又发生变化时会停止撤销，避免覆盖新内容。
- 新建科研项目或实验计划后会跳转到对应页面；过程记录和文件操作会定位到实际发生变化的工作区。

### 实验执行与过程记录

- 实验计划详情只承担“概览 / 实验方案 / 实验执行”，不再把计划、执行和记录平铺在同一页面。
- 新建实验计划不会隐式产生空执行；用户开始实际工作时再创建一次实验执行。
- 每次实验执行独立拥有执行编号、重复类型、实际日期、实际参数、样本使用、状态、摘要和结论。
- 实验方案中的“方案阶段”只保存研究顺序、阶段目标、负责人和目标日期；实验批次中的“实验步骤”从空清单独立创建，双方不复制、不同步、不回写。
- 实验批次支持独立新增、编辑、删除和上下排序实验步骤；批量日期管理支持统一日期、按间隔顺排、整体偏移和清空安排日期。
- 新增过程记录、记录模板调用、过程时间线和批量管理集中到所属实验执行页面。
- 记录模板在模板中心和详情页直接选择目标实验执行并打开新增过程记录表单，不再经过实验计划页二次选择。
- 默认执行编号统一为 `RUN-01`；创建过程记录时保留校验失败、普通保存和带文件保存各自正确的返回位置。
- 每条活动过程记录必须归属于当前实验计划的一次活动执行；跨计划、跨执行和已删除父级请求会被拒绝。
- 记录详情明确返回所属实验执行；文件继续归入产生它的具体过程记录。

### 旧数据迁移与完整性

- 科研工作区基础迁移为 `a4d8f6c2e701`，旧记录修复为 `e6b9c1d4f208`，API 模型能力持久化为 `7c3d9a1e5f42`，执行与记录层级收紧为 `c9a4e7d2b610`，AI 执行范围持久化为 `d2f8a1c7b604`，执行步骤实例化为 `d1f3a5b7c902`；迁移链当前只有一个 head。
- 所有过程记录（包括回收站记录）若执行为空、执行不存在、指向其他实验或指向已删除执行，会按所属实验归入自动创建的 `HISTORY-LEGACY` 历史执行；编号冲突时自动增加后缀。
- 实验状态“完成”会映射为执行状态“已完成”，未知旧状态回落为“未开始”，不会写入非法执行状态。
- 回收站中的过程记录不会被无意义迁出；迁移可重复运行且不会重复创建历史执行。
- 修复完成后把 `experiment_record.batch_id` 收紧为 `NOT NULL`，并从实验计划表删除重复的执行编号、重复类型、重复序号和分组字段。
- 有过程记录的执行会自动校正为“进行中”，并扩展实际日期范围以覆盖既有记录，不会丢失旧结束日期。
- 旧版升级仍会把历史计划步骤恢复到已有实验执行并保留完成状态；这是一次性兼容迁移，新建批次不再复制方案阶段。
- 项目包导出会先核对过程记录归属；发现异常时停止生成并提示先升级，避免产生看似成功但遗漏数据的 `.ralab` 文件。

### 导出一致性

- JSON 导出 schema v3 中，“方案阶段”只保留方案字段，每个实验批次独立导出实验步骤和完成状态，并继续保留带批次编号的平铺 `records` 兼容视图。
- 无法归档的活动旧记录会进入明确的 `unassigned_records / HISTORY-UNASSIGNED` 兼容分组，常规导出不会静默丢数据。
- Markdown、Word 和 Excel 均按“实验执行 → 过程记录 → 文件”组织；Word 标题和 Excel 工作表统一使用新术语。
- Excel 新增“实验执行”工作表，“过程记录”“过程记录参数”和“附件清单”均包含执行编号。
- ZIP 中的 Markdown、JSON、文件清单和附件目录共用同一份过滤后快照，附件路径按执行编号分层。
- `.ralab` 项目包 schema v2 分别保存方案阶段与每个批次的实验步骤；导入仍兼容旧 schema v1 项目包及历史来源字段。
- 所有格式过滤已移入回收站的过程记录和附件；ZIP 不再打包软删除文件。

### 维护者变化

- 领域词汇写入 `CONTEXT.md`，计划、执行和过程记录的边界记录在 `docs/adr/0001-separate-plans-executions-and-records.md`。
- 导出共享结构集中到 `app/export_service.py`，项目包完整性检查位于 `app/project_package.py`。
- 新增旧记录修复、项目包完整性和多格式导出测试；API、助手和工作区测试同步覆盖本轮边界。

### 验证结果

- `.venv\Scripts\python.exe -m pytest -q`：`149 passed, 9 warnings in 136.95s`
- `.venv\Scripts\python.exe -m compileall -q app migrations tests scripts packaging ...`：通过
- `node --check app\static\js\app.js`：通过
- `node --check scripts\build_weekly_presentation.mjs`：通过
- `.venv\Scripts\python.exe -m flask --app run.py db heads`：`d1f3a5b7c902 (head)`
- `git diff --check`：通过（仅有 Windows 换行提示）

pytest 结束时 Windows 临时目录可能出现非致命 `WinError 5` 清理提示；应以 pytest 进程退出码和测试汇总为准。Alembic 环境的弃用 warning 不影响迁移执行，但后续应单独升级 `migrations/env.py` 的引擎访问方式。
